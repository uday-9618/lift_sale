import 'package:flutter/material.dart';

class Akshaya extends StatelessWidget {
  const Akshaya({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Akshaya')),
      body: Center(child: Text('Akshaya')),
    );
  }
}
