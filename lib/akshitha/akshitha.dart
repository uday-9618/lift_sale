import 'package:flutter/material.dart';

class Akshitha extends StatelessWidget {
  const Akshitha({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Akshitha')),
      body: Center(child: Text('Akshitha')),
    );
  }
}
